Use the single app icon at:

  ../icon.png   (project root "Smart cam", next to mobile/ and desktop/)

Do not maintain a separate copy here. Regenerate mobile launcher icons after changing it:

  cd mobile && dart run flutter_launcher_icons
